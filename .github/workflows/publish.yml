name: Sync Backend to Hugging Face Hub
on:
  push:
    branches: [backend]
    paths:
      - 'backend/**'
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Push backend subdirectory to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          git config --global user.email "ci@github.com"
          git config --global user.name "GitHub Actions"

          # Clone the HF repo
          git clone https://user:${HF_TOKEN}@huggingface.co/mallik-amaan/edubot-offline hf_repo

          # Sync only the backend folder into it
          rsync -av --delete --exclude='.git' backend/ hf_repo/

          # Commit and push
          cd hf_repo
          git add -A
          git diff --cached --quiet || git commit -m "Sync backend from GitHub @ ${{ github.sha }}"
          git push
